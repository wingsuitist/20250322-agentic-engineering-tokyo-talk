# Prompts used

The following prompts where generated talking to [superwhisper](https://superwhisper.com/) in a interactive "vibe coding session".w

## Write the README.md:
Let's build a web application which creates mermaid diagrams based on a user prompt.  We'll now start to just write the README markdown, README MD,  and we try to keep it concise and minimal and make sure that it only contains the information,  technical information needed to actually build the application so we can discuss about it.  We need a frontend single page application and I want for now an input field for the user prompt,  the generation button and I want a box which shows the mermaid diagram rendered  and I want to have a box with copy-pasteable code snippet which is the mermaid syntax.  Then of course we need a backend, REST API backend which basically gets the prompt from the frontend,  goes to openrouter and fetches the answer from the model and then forwards this syntax to the frontend.  We need a system prompt which we can tune which basically tells the model how to generate the nicely colored mermaid diagrams  using it to the fullest function and also making sure that it's only generating mermaid diagrams.  So, yeah, just start with writing that.  So, yeah, just start with writing that.


## build backend:

so let's build the backend now build the backend tell me how to run it I want to run it and also test it yourself with basic curl commands before you write tests

'README.md' (see below for file content)


## build frontend:

now build the front end without styes just make it work according to the readme

'README.md' (see below for file content)


## find a proper styling library:

The frontend and backend are now working but we need to style the frontend.  But for that we need to select a UI CSS component library which works well with Alpine GIS.  I want something very very simple, something that you know well so you can write the code and preferably maybe use an MCP to research the best models for that use case.  And let's see what it tells you.


## style it with pico css:

the frontend is done now use pico css to style it

@/frontend/@/README.md